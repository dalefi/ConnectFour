# Installation: siehe README.
#
# WICHTIG: diese Datei NICHT mit --index-url gegen den PyTorch-Index installieren.
# --index-url ersetzt PyPI komplett, und dort liegen nur torch-Pakete - psycopg2,
# sqlalchemy usw. sind dann nicht auffindbar. Fuer CUDA zuerst torch vom
# PyTorch-Index installieren, danach diese Datei ganz normal von PyPI:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
#   pip install -r requirements.txt

sqlalchemy
psycopg2
numpy==2.3.4
matplotlib==3.10.7
tqdm
torch

# torchvision und torchaudio werden im Projekt nirgends importiert und ziehen
# mehrere hundert MB. Bei Bedarf einfach wieder einkommentieren.
# torchvision
# torchaudio

# Perfekter Solver als absoluter Massstab (src/solver_benchmark.py).
# NICHT hier eintragen: es gibt nur ein einziges Wheel (CPython 3.13, Windows
# x86-64) und kein sdist. Auf jeder anderen Kombination bricht die Installation
# der gesamten Datei ab. Separat installieren, wenn die Plattform passt:
#
#   pip install connect-four-ai
#
# src/solver_benchmark.py importiert das Paket erst bei Benutzung, die Tests
# ueberspringen sich ohne es.

# Tests
pytest
pytest-asyncio
