[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
markers =
    slow: tests that run a full MCTS search or training step
filterwarnings =
    error::RuntimeWarning
