services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: daniel
      POSTGRES_PASSWORD: connectfour
      POSTGRES_DB: connectfour
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
